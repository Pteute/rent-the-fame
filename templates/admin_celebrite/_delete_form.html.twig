<form method="post" action="{{ path('admin_celebrite_delete', {'id': celebrite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ celebrite.id) }}">
    <button class="btn btn-dark">Supprimer</button>
</form>
