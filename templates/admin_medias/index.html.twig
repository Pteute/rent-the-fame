{% extends 'admin.html.twig' %}

{% block title %}Medias index{% endblock %}

{% block body %}
    <h1>Medias index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Name</th>
                <th>Url</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for media in medias %}
            <tr>
                <td>{{ media.id }}</td>
                <td>{{ media.type }}</td>
                <td>{{ media.name }}</td>
                <td>{{ media.url }}</td>
                <td>{{ media.celebrite.nom }}</td>
                <td>
                    <a href="{{ path('admin_medias_show', {'id': media.id}) }}">show</a>
                    <a href="{{ path('admin_medias_edit', {'id': media.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('admin_medias_new') }}">Create new</a>
{% endblock %}
