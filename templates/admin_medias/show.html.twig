{% extends 'admin.html.twig' %}

{% block title %}Medias{% endblock %}

{% block body %}
    <h1>Medias</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ media.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ media.type }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ media.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ media.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_medias_index') }}">back to list</a>

    <a href="{{ path('admin_medias_edit', {'id': media.id}) }}">edit</a>

    {{ include('admin_medias/_delete_form.html.twig') }}
{% endblock %}
