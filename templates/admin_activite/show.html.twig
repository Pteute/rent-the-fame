{% extends 'admin.html.twig' %}

{% block title %}Activite{% endblock %}

{% block body %}
    <h1>Activite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ activite.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ activite.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_activite_index') }}">back to list</a>

    <a href="{{ path('admin_activite_edit', {'id': activite.id}) }}">edit</a>

    {{ include('admin_activite/_delete_form.html.twig') }}
{% endblock %}
